//tuka pishem gamescript kod
//надявам се утре не се инатиш толкова и вземем да си говориме че е мн неудобно така :D

int a = "pesho";
//gs2cpp: Error on line 5: Expected integer, got string;

scene s;
s = currentScene;

if YouMustCallMeTomorrowOrIWillUnFriendYou == true
{
//	false    DAMN! :D tva ne znae true/false :D
	Window w = "Second window";
	layer l = new layer("test layer");
	w.addLayer(l);
	w.show();
	real r = pi*180;
	messagebox("Error!!!!");

	draw.setColor("#FF0000");
	draw.cube(0,0,0,10,10,10);
	draw.setColor(color.white);
	draw.line(0,0,currentLayer.width, currentLayer.height);

	model m = "file.3ds";
	m.mapTexture("file.jpg");
	draw.model(m,x,y,z);

	//same example with other syntax:
	image t; 
	t.load("file.jpg");
	model m;
	m.load("file.3ds");
	m.mapTexture(t);
	draw.to(currentWindow.getLayer("scoreboard"));
	//draw.model(m,10,10);
	m.drawScaled(x,y,z,1,1,1);
}

string[] scores;
score s = 200;
s.setName("Iliqn");
scores << s
scores.append(s);


//
//tuka pishem gamescript kod
//надявам се утре не се инатиш толкова и вземем да си говориме че е мн неудобно така :D

int a = "pesho";
//gs2cpp: Error on line 5: Expected integer, got string;

scene s;
s = currentScene;

if YouMustCallMeTomorrowOrIWillUnFriendYou == true
{
//	false    DAMN! :D tva ne znae true/false :D
	Window w = "Second window";
	layer l = new layer("test layer");
	w.addLayer(l);
	w.show();
	real r = pi*180;
	messagebox("Error!!!!");

	draw.setColor("#FF0000");
	draw.cube(0,0,0,10,10,10);
	draw.setColor(color.white);
	draw.line(0,0,currentLayer.width, currentLayer.height);

	model m = "file.3ds";
	m.mapTexture("file.jpg");
	draw.model(m,x,y,z);

	//same example with other syntax:
	image t; 
	t.load("file.jpg");
	model m;
	m.load("file.3ds");
	m.mapTexture(t);
	draw.to(currentWindow.getLayer("scoreboard"));
	//draw.model(m,10,10);
	m.drawScaled(x,y,z,1,1,1);
}

string[] scores;
score s = 200;
s.setName("Iliqn");
scores << s
scores.append(s);


//
//tuka pishem gamescript kod
//надявам се утре не се инатиш толкова и вземем да си говориме че е мн неудобно така :D

int a = "pesho";
//gs2cpp: Error on line 5: Expected integer, got string;

scene s;
s = currentScene;

if YouMustCallMeTomorrowOrIWillUnFriendYou == true
{
//	false    DAMN! :D tva ne znae true/false :D
	Window w = "Second window";
	layer l = new layer("test layer");
	w.addLayer(l);
	w.show();
	real r = pi*180;
	messagebox("Error!!!!");

	draw.setColor("#FF0000");
	draw.cube(0,0,0,10,10,10);
	draw.setColor(color.white);
	draw.line(0,0,currentLayer.width, currentLayer.height);

	model m = "file.3ds";
	m.mapTexture("file.jpg");
	draw.model(m,x,y,z);

	//same example with other syntax:
	image t; 
	t.load("file.jpg");
	model m;
	m.load("file.3ds");
	m.mapTexture(t);
	draw.to(currentWindow.getLayer("scoreboard"));
	//draw.model(m,10,10);
	m.drawScaled(x,y,z,1,1,1);
}

string[] scores;
score s = 200;
s.setName("Iliqn");
scores << s
scores.append(s);


//
//tuka pishem gamescript kod
//надявам се утре не се инатиш толкова и вземем да си говориме че е мн неудобно така :D

int a = "pesho";
//gs2cpp: Error on line 5: Expected integer, got string;

scene s;
s = currentScene;

if YouMustCallMeTomorrowOrIWillUnFriendYou == true
{
//	false    DAMN! :D tva ne znae true/false :D
	Window w = "Second window";
	layer l = new layer("test layer");
	w.addLayer(l);
	w.show();
	real r = pi*180;
	messagebox("Error!!!!");

	draw.setColor("#FF0000");
	draw.cube(0,0,0,10,10,10);
	draw.setColor(color.white);
	draw.line(0,0,currentLayer.width, currentLayer.height);

	model m = "file.3ds";
	m.mapTexture("file.jpg");
	draw.model(m,x,y,z);

	//same example with other syntax:
	image t; 
	t.load("file.jpg");
	model m;
	m.load("file.3ds");
	m.mapTexture(t);
	draw.to(currentWindow.getLayer("scoreboard"));
	//draw.model(m,10,10);
	m.drawScaled(x,y,z,1,1,1);
}

string[] scores;
score s = 200;
s.setName("Iliqn");
scores << s
scores.append(s);


//
//tuka pishem gamescript kod
//надявам се утре не се инатиш толкова и вземем да си говориме че е мн неудобно така :D

int a = "pesho";
//gs2cpp: Error on line 5: Expected integer, got string;

scene s;
s = currentScene;

if YouMustCallMeTomorrowOrIWillUnFriendYou == true
{
//	false    DAMN! :D tva ne znae true/false :D
	Window w = "Second window";
	layer l = new layer("test layer");
	w.addLayer(l);
	w.show();
	real r = pi*180;
	messagebox("Error!!!!");

	draw.setColor("#FF0000");
	draw.cube(0,0,0,10,10,10);
	draw.setColor(color.white);
	draw.line(0,0,currentLayer.width, currentLayer.height);

	model m = "file.3ds";
	m.mapTexture("file.jpg");
	draw.model(m,x,y,z);

	//same example with other syntax:
	image t; 
	t.load("file.jpg");
	model m;
	m.load("file.3ds");
	m.mapTexture(t);
	draw.to(currentWindow.getLayer("scoreboard"));
	//draw.model(m,10,10);
	m.drawScaled(x,y,z,1,1,1);
}

string[] scores;
score s = 200;
s.setName("Iliqn");
scores << s
scores.append(s);


//
//tuka pishem gamescript kod
//надявам се утре не се инатиш толкова и вземем да си говориме че е мн неудобно така :D

int a = "pesho";
//gs2cpp: Error on line 5: Expected integer, got string;

scene s;
s = currentScene;

if YouMustCallMeTomorrowOrIWillUnFriendYou == true
{
//	false    DAMN! :D tva ne znae true/false :D
	Window w = "Second window";
	layer l = new layer("test layer");
	w.addLayer(l);
	w.show();
	real r = pi*180;
	messagebox("Error!!!!");

	draw.setColor("#FF0000");
	draw.cube(0,0,0,10,10,10);
	draw.setColor(color.white);
	draw.line(0,0,currentLayer.width, currentLayer.height);

	model m = "file.3ds";
	m.mapTexture("file.jpg");
	draw.model(m,x,y,z);

	//same example with other syntax:
	image t; 
	t.load("file.jpg");
	model m;
	m.load("file.3ds");
	m.mapTexture(t);
	draw.to(currentWindow.getLayer("scoreboard"));
	//draw.model(m,10,10);
	m.drawScaled(x,y,z,1,1,1);
}

string[] scores;
score s = 200;
s.setName("Iliqn");
scores << s
scores.append(s);


//
//tuka pishem gamescript kod
//надявам се утре не се инатиш толкова и вземем да си говориме че е мн неудобно така :D

int a = "pesho";
//gs2cpp: Error on line 5: Expected integer, got string;

scene s;
s = currentScene;

if YouMustCallMeTomorrowOrIWillUnFriendYou == true
{
//	false    DAMN! :D tva ne znae true/false :D
	Window w = "Second window";
	layer l = new layer("test layer");
	w.addLayer(l);
	w.show();
	real r = pi*180;
	messagebox("Error!!!!");

	draw.setColor("#FF0000");
	draw.cube(0,0,0,10,10,10);
	draw.setColor(color.white);
	draw.line(0,0,currentLayer.width, currentLayer.height);

	model m = "file.3ds";
	m.mapTexture("file.jpg");
	draw.model(m,x,y,z);

	//same example with other syntax:
	image t; 
	t.load("file.jpg");
	model m;
	m.load("file.3ds");
	m.mapTexture(t);
	draw.to(currentWindow.getLayer("scoreboard"));
	//draw.model(m,10,10);
	m.drawScaled(x,y,z,1,1,1);
}

string[] scores;
score s = 200;
s.setName("Iliqn");
scores << s
scores.append(s);


//
//tuka pishem gamescript kod
//надявам се утре не се инатиш толкова и вземем да си говориме че е мн неудобно така :D

int a = "pesho";
//gs2cpp: Error on line 5: Expected integer, got string;

scene s;
s = currentScene;

if YouMustCallMeTomorrowOrIWillUnFriendYou == true
{
//	false    DAMN! :D tva ne znae true/false :D
	Window w = "Second window";
	layer l = new layer("test layer");
	w.addLayer(l);
	w.show();
	real r = pi*180;
	messagebox("Error!!!!");

	draw.setColor("#FF0000");
	draw.cube(0,0,0,10,10,10);
	draw.setColor(color.white);
	draw.line(0,0,currentLayer.width, currentLayer.height);

	model m = "file.3ds";
	m.mapTexture("file.jpg");
	draw.model(m,x,y,z);

	//same example with other syntax:
	image t; 
	t.load("file.jpg");
	model m;
	m.load("file.3ds");
	m.mapTexture(t);
	draw.to(currentWindow.getLayer("scoreboard"));
	//draw.model(m,10,10);
	m.drawScaled(x,y,z,1,1,1);
}

string[] scores;
score s = 200;
s.setName("Iliqn");
scores << s
scores.append(s);


//
//tuka pishem gamescript kod
//надявам се утре не се инатиш толкова и вземем да си говориме че е мн неудобно така :D

int a = "pesho";
//gs2cpp: Error on line 5: Expected integer, got string;

scene s;
s = currentScene;

if YouMustCallMeTomorrowOrIWillUnFriendYou == true
{
//	false    DAMN! :D tva ne znae true/false :D
	Window w = "Second window";
	layer l = new layer("test layer");
	w.addLayer(l);
	w.show();
	real r = pi*180;
	messagebox("Error!!!!");

	draw.setColor("#FF0000");
	draw.cube(0,0,0,10,10,10);
	draw.setColor(color.white);
	draw.line(0,0,currentLayer.width, currentLayer.height);

	model m = "file.3ds";
	m.mapTexture("file.jpg");
	draw.model(m,x,y,z);

	//same example with other syntax:
	image t; 
	t.load("file.jpg");
	model m;
	m.load("file.3ds");
	m.mapTexture(t);
	draw.to(currentWindow.getLayer("scoreboard"));
	//draw.model(m,10,10);
	m.drawScaled(x,y,z,1,1,1);
}

string[] scores;
score s = 200;
s.setName("Iliqn");
scores << s
scores.append(s);


//
//tuka pishem gamescript kod
//надявам се утре не се инатиш толкова и вземем да си говориме че е мн неудобно така :D

int a = "pesho";
//gs2cpp: Error on line 5: Expected integer, got string;

scene s;
s = currentScene;

if YouMustCallMeTomorrowOrIWillUnFriendYou == true
{
//	false    DAMN! :D tva ne znae true/false :D
	Window w = "Second window";
	layer l = new layer("test layer");
	w.addLayer(l);
	w.show();
	real r = pi*180;
	messagebox("Error!!!!");

	draw.setColor("#FF0000");
	draw.cube(0,0,0,10,10,10);
	draw.setColor(color.white);
	draw.line(0,0,currentLayer.width, currentLayer.height);

	model m = "file.3ds";
	m.mapTexture("file.jpg");
	draw.model(m,x,y,z);

	//same example with other syntax:
	image t; 
	t.load("file.jpg");
	model m;
	m.load("file.3ds");
	m.mapTexture(t);
	draw.to(currentWindow.getLayer("scoreboard"));
	//draw.model(m,10,10);
	m.drawScaled(x,y,z,1,1,1);
}

string[] scores;
score s = 200;
s.setName("Iliqn");
scores << s
scores.append(s);


//
//tuka pishem gamescript kod
//надявам се утре не се инатиш толкова и вземем да си говориме че е мн неудобно така :D

int a = "pesho";
//gs2cpp: Error on line 5: Expected integer, got string;

scene s;
s = currentScene;

if YouMustCallMeTomorrowOrIWillUnFriendYou == true
{
//	false    DAMN! :D tva ne znae true/false :D
	Window w = "Second window";
	layer l = new layer("test layer");
	w.addLayer(l);
	w.show();
	real r = pi*180;
	messagebox("Error!!!!");

	draw.setColor("#FF0000");
	draw.cube(0,0,0,10,10,10);
	draw.setColor(color.white);
	draw.line(0,0,currentLayer.width, currentLayer.height);

	model m = "file.3ds";
	m.mapTexture("file.jpg");
	draw.model(m,x,y,z);

	//same example with other syntax:
	image t; 
	t.load("file.jpg");
	model m;
	m.load("file.3ds");
	m.mapTexture(t);
	draw.to(currentWindow.getLayer("scoreboard"));
	//draw.model(m,10,10);
	m.drawScaled(x,y,z,1,1,1);
}

string[] scores;
score s = 200;
s.setName("Iliqn");
scores << s
scores.append(s);


//
//tuka pishem gamescript kod
//надявам се утре не се инатиш толкова и вземем да си говориме че е мн неудобно така :D

int a = "pesho";
//gs2cpp: Error on line 5: Expected integer, got string;

scene s;
s = currentScene;

if YouMustCallMeTomorrowOrIWillUnFriendYou == true
{
//	false    DAMN! :D tva ne znae true/false :D
	Window w = "Second window";
	layer l = new layer("test layer");
	w.addLayer(l);
	w.show();
	real r = pi*180;
	messagebox("Error!!!!");

	draw.setColor("#FF0000");
	draw.cube(0,0,0,10,10,10);
	draw.setColor(color.white);
	draw.line(0,0,currentLayer.width, currentLayer.height);

	model m = "file.3ds";
	m.mapTexture("file.jpg");
	draw.model(m,x,y,z);

	//same example with other syntax:
	image t; 
	t.load("file.jpg");
	model m;
	m.load("file.3ds");
	m.mapTexture(t);
	draw.to(currentWindow.getLayer("scoreboard"));
	//draw.model(m,10,10);
	m.drawScaled(x,y,z,1,1,1);
}

string[] scores;
score s = 200;
s.setName("Iliqn");
scores << s
scores.append(s);


//
//tuka pishem gamescript kod
//надявам се утре не се инатиш толкова и вземем да си говориме че е мн неудобно така :D

int a = "pesho";
//gs2cpp: Error on line 5: Expected integer, got string;

scene s;
s = currentScene;

if YouMustCallMeTomorrowOrIWillUnFriendYou == true
{
//	false    DAMN! :D tva ne znae true/false :D
	Window w = "Second window";
	layer l = new layer("test layer");
	w.addLayer(l);
	w.show();
	real r = pi*180;
	messagebox("Error!!!!");

	draw.setColor("#FF0000");
	draw.cube(0,0,0,10,10,10);
	draw.setColor(color.white);
	draw.line(0,0,currentLayer.width, currentLayer.height);

	model m = "file.3ds";
	m.mapTexture("file.jpg");
	draw.model(m,x,y,z);

	//same example with other syntax:
	image t; 
	t.load("file.jpg");
	model m;
	m.load("file.3ds");
	m.mapTexture(t);
	draw.to(currentWindow.getLayer("scoreboard"));
	//draw.model(m,10,10);
	m.drawScaled(x,y,z,1,1,1);
}

string[] scores;
score s = 200;
s.setName("Iliqn");
scores << s
scores.append(s);


//
//tuka pishem gamescript kod
//надявам се утре не се инатиш толкова и вземем да си говориме че е мн неудобно така :D

int a = "pesho";
//gs2cpp: Error on line 5: Expected integer, got string;

scene s;
s = currentScene;

if YouMustCallMeTomorrowOrIWillUnFriendYou == true
{
//	false    DAMN! :D tva ne znae true/false :D
	Window w = "Second window";
	layer l = new layer("test layer");
	w.addLayer(l);
	w.show();
	real r = pi*180;
	messagebox("Error!!!!");

	draw.setColor("#FF0000");
	draw.cube(0,0,0,10,10,10);
	draw.setColor(color.white);
	draw.line(0,0,currentLayer.width, currentLayer.height);

	model m = "file.3ds";
	m.mapTexture("file.jpg");
	draw.model(m,x,y,z);

	//same example with other syntax:
	image t; 
	t.load("file.jpg");
	model m;
	m.load("file.3ds");
	m.mapTexture(t);
	draw.to(currentWindow.getLayer("scoreboard"));
	//draw.model(m,10,10);
	m.drawScaled(x,y,z,1,1,1);
}

string[] scores;
score s = 200;
s.setName("Iliqn");
scores << s
scores.append(s);


//
//tuka pishem gamescript kod
//надявам се утре не се инатиш толкова и вземем да си говориме че е мн неудобно така :D

int a = "pesho";
//gs2cpp: Error on line 5: Expected integer, got string;

scene s;
s = currentScene;

if YouMustCallMeTomorrowOrIWillUnFriendYou == true
{
//	false    DAMN! :D tva ne znae true/false :D
	Window w = "Second window";
	layer l = new layer("test layer");
	w.addLayer(l);
	w.show();
	real r = pi*180;
	messagebox("Error!!!!");

	draw.setColor("#FF0000");
	draw.cube(0,0,0,10,10,10);
	draw.setColor(color.white);
	draw.line(0,0,currentLayer.width, currentLayer.height);

	model m = "file.3ds";
	m.mapTexture("file.jpg");
	draw.model(m,x,y,z);

	//same example with other syntax:
	image t; 
	t.load("file.jpg");
	model m;
	m.load("file.3ds");
	m.mapTexture(t);
	draw.to(currentWindow.getLayer("scoreboard"));
	//draw.model(m,10,10);
	m.drawScaled(x,y,z,1,1,1);
}

string[] scores;
score s = 200;
s.setName("Iliqn");
scores << s
scores.append(s);


//
//tuka pishem gamescript kod
//надявам се утре не се инатиш толкова и вземем да си говориме че е мн неудобно така :D

int a = "pesho";
//gs2cpp: Error on line 5: Expected integer, got string;

scene s;
s = currentScene;

if YouMustCallMeTomorrowOrIWillUnFriendYou == true
{
//	false    DAMN! :D tva ne znae true/false :D
	Window w = "Second window";
	layer l = new layer("test layer");
	w.addLayer(l);
	w.show();
	real r = pi*180;	
	messagebox("Error!!!!");

	draw.setColor("#FF0000");
	draw.cube(0,0,0,10,10,10);
	draw.setColor(color.white);
	draw.line(0,0,currentLayer.width, currentLayer.height);

	model m = "file.3ds";
	m.mapTexture("file.jpg");
	draw.model(m,x,y,z);

	//same example with other syntax:
	image t; 
	t.load("file.jpg");
	model m;
	m.load("file.3ds");
	m.mapTexture(t);
	draw.to(currentWindow.getLayer("scoreboard"));
	//draw.model(m,10,10);
	m.drawScaled(x,y,z,1,1,1);
}
